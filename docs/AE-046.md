# AE-046 — Render PDF Pages to Page Images

## Objective

Render each PDF page into a normalized page image and persist the corresponding page documents as the per-page ingestion assets.

This ticket replaces the current lightweight/stub implementation in `PageIngestionService.processPage` with real PDF page rendering and establishes the canonical normalized page-image representation that AE-048 will also use for direct image inputs.

## Scope

- Process each page of an uploaded PDF individually.
- Produce one normalized image asset per PDF page.
- Persist the corresponding page document for each extracted page.
- Replace the current lightweight page-extraction placeholder in `PageIngestionService.processPage`.
- Establish the canonical normalized page-image representation (format, dimensions/resolution and storage representation) for reuse by AE-048.
- Integrate with the existing ingestion pipeline without changing the upload contract.
- Preserve deterministic page numbering and existing idempotency behaviour.
- Use the actual PDF renderer's page count as the authoritative processing count, while retaining the existing upload-time page-count check as a cheap pre-flight guard.
- Bound rendering cost with a target DPI or maximum long-edge and a hard maximum output-pixel limit per page.
- Render pages sequentially or with bounded concurrency rather than unbounded `Promise.all`.
- Preserve the existing per-page `executeWithTimeout` protection.
- Keep rendering failures sanitized and remove fabricated/nonexistent stack traces.

## TODOs

### 1. Existing Flow and Dependencies

- [ ] Inspect the existing ingestion flow, `PageIngestionService`, `IngestionPage`, storage utilities, page-count validation, and worker timeout/idempotency behaviour before implementation.
- [ ] Evaluate whether the existing `sharp` dependency can be reused for image normalization/resizing before introducing another image library.
- [ ] Choose either `pdfjs-dist` or `pdf2pic` for PDF rendering and document the reason for the choice.
- [ ] Verify the selected rendering approach works with `npm ci` on Linux/`ubuntu-latest`.
- [ ] Ensure the lockfile contains the required Linux/native bindings rather than relying on machine-specific dependencies.
- [ ] If system packages are required by the selected renderer, add the required `apt-get` installation step to `.github/workflows/ci.yml` in the same PR.

### 2. PDF Page Rendering

- [ ] Replace the current lightweight `processPage` placeholder with real PDF page rendering.
- [ ] Define and document the canonical normalized page-image representation so AE-048 can reuse it for image inputs.
- [ ] Render every PDF page into that normalized representation.
- [ ] Store each generated page image through the existing storage approach.
- [ ] Create the corresponding `IngestionPage` document with the correct batch/file association and deterministic page number.

### 3. Page Count

- [ ] Retain the existing regex-based page count as the upload-time guard.
- [ ] Treat the renderer's page count as authoritative once the PDF is opened.
- [ ] Reconcile `totalPages` with the renderer's actual page count.
- [ ] Ensure processing can reach a terminal state when the upload-time estimate and renderer count differ.

### 4. Rendering Safety

- [ ] Add a target DPI or maximum long-edge constraint.
- [ ] Add a hard maximum output-pixel limit per rendered page.
- [ ] Render pages sequentially or using bounded concurrency.
- [ ] Do not use unbounded `Promise.all` across all pages.
- [ ] Preserve the existing per-page `executeWithTimeout` behaviour.

### 5. Testability and Failure Handling

- [ ] Remove `simulatePageFailure` and `simulateHungPage` from the production method signature.
- [ ] Introduce an injectable renderer/dependency that tests can substitute instead of production-only test flags.
- [ ] Remove the fabricated `PageRenderer.render` stack trace.
- [ ] Ensure actual rendering errors are sanitized and meaningful.
- [ ] Add focused tests for single-page and multi-page PDF extraction.
- [ ] Add tests covering page-count reconciliation.
- [ ] Add tests covering the per-page output-pixel/rendering limits.
- [ ] Add tests using the injectable renderer for rendering failure/timeout scenarios.

### 6. Verification

- [ ] Verify `npm ci` and the rendering dependency on Linux/CI.
- [ ] Run relevant Vitest tests.
- [ ] Run the complete test suite.
- [ ] Run `npx tsc --noEmit`.
- [ ] Run `npm run lint`.

## Acceptance Criteria

- A valid single-page PDF produces exactly one normalized stored page image and one corresponding `IngestionPage` document.
- A valid multi-page PDF produces one normalized stored page image and one corresponding `IngestionPage` document for every page reported by the actual renderer.
- Page numbering is deterministic and each page remains associated with the correct batch and source file.
- Reprocessing/retrying a page does not create duplicate page records or assets under the existing idempotency rules.
- The existing upload-time page-count check remains as a cheap pre-flight guard.
- The renderer's page count is authoritative for actual processing and `totalPages` is reconciled accordingly.
- Rendering cannot exceed the configured per-page output-pixel limit.
- Pages are rendered sequentially or with bounded concurrency.
- The existing per-page timeout remains effective.
- Production code does not expose `simulatePageFailure` or `simulateHungPage` test-only flags.
- Tests can substitute the renderer without modifying production behaviour.
- Rendering failures contain sanitized, meaningful error information and no fabricated stack frames.
- The selected rendering dependency installs and runs successfully in the Linux CI environment.
- ESLint, TypeScript, and relevant tests pass.

## Out of Scope

- Thumbnail generation (AE-047).
- Mixed-input handling beyond establishing the canonical page representation required by AE-048.
- Dedicated broad extraction/storage fixture coverage (AE-049).
- UI implementation for displaying page images or thumbnails.
- Changes to unrelated ingestion, authentication, authorization, or security behaviour.

## Sequencing

The Week 3 Day 2 sequence is:

**AE-046 → AE-047 → AE-048 → AE-049**

AE-046 establishes the canonical normalized page-image representation that AE-048 will reuse for direct image inputs.

## Working Protocol

- Work from the approved `week3` milestone branch.
- Keep commits small and focused.
- Keep ESLint and tests green.
- Verify TypeScript compilation before opening the PR.
- Demo at the Week 3 checkpoint.
- If a bug is found, use the Bug format:

  **Problem → What's happening → Error in logs → Where to look**
