# AE-049 — Tests for Page Extraction + Storage

## Objective

Add fixture-based integration coverage for the PDF/image ingestion pipeline, verifying that real input files pass through the full ingestion flow and produce the expected page counts, stored assets, Page documents, ordering, and failure/idempotency behaviour.

## Scope

- Add small, static PDF and image fixtures under `src/__tests__/fixtures/`.
- Run real fixture bytes through the full ingestion pipeline.
- Verify expected page counts.
- Verify normalized page assets are stored.
- Verify corresponding Page documents are created correctly.
- Verify deterministic `(fileIndex, pageNumber)` ordering.
- Verify source batch/file associations.
- Verify relevant failure and idempotency paths.
- Add dedicated coverage for PDF page-count reconciliation between the upload-time guard and the authoritative renderer count.
- Verify fixture files are tracked by Git and are not silently excluded by ignore rules.
- Verify test runs do not leave stray runtime artifacts under `data/`.

## Test Boundary

AE-049 owns the **fixture-based integration suite**.

The individual feature tickets own focused unit tests for their own logic:

- **AE-046** — unit tests for PDF page rendering and rendering-related logic using small/in-memory inputs.
- **AE-047** — unit tests for thumbnail generation and normalization.
- **AE-048** — unit tests for image normalization and mixed-input handling.
- **AE-049** — real static PDF/image fixtures through the full ingestion pipeline.

This separation prevents duplicate coverage while ensuring the complete pipeline is tested with real file bytes.

## TODOs

### 1. Fixture Design

- [x] Inspect the existing test setup and ingestion pipeline before creating fixtures.
- [x] Add small static PDF fixtures under `src/__tests__/fixtures/`.
- [x] Add small static image fixtures under `src/__tests__/fixtures/`.
- [x] Keep fixtures genuinely small, preferably only a few KB each.
- [x] Record the known-correct page count for every PDF fixture as a literal in the tests.
- [x] Do not generate PDF fixtures at test runtime using the same library used by the production renderer.
- [x] Ensure fixture files are not excluded by `.gitignore`.
- [x] Run `git check-ignore` against each fixture before relying on it in CI.
- [x] Verify the fixtures are actually tracked by Git.

### 2. Full Pipeline Integration Tests

- [x] Pass a real single-page PDF fixture through the full ingestion pipeline.
- [x] Pass a real multi-page PDF fixture through the full ingestion pipeline.
- [x] Pass a real image fixture through the full ingestion pipeline.
- [x] Pass a multi-file/mixed PDF and image fixture set through the full ingestion pipeline.
- [x] Assert the expected number of pages is produced.
- [x] Assert normalized page assets are stored.
- [x] Assert corresponding Page documents are created.
- [x] Assert Page documents contain the correct batch/file associations.
- [x] Assert deterministic `(fileIndex, pageNumber)` ordering.
- [x] Assert standalone image inputs use `pageNumber = 1`.
- [x] Assert stored derived assets use deterministic keys.
- [x] Assert retries/idempotent processing do not create duplicate Page documents or stored assets.

### 3. Failure and Idempotency Coverage

- [x] Add fixture-based coverage for relevant invalid/corrupt input behaviour.
- [x] Verify an invalid file in a multi-file upload follows the AE-048 reject-whole-batch decision.
- [x] Verify the failure does not leave partially ingested files/pages.
- [x] Verify reprocessing the same valid batch preserves identical ordering.
- [x] Verify retries do not create duplicate Page documents or derived assets.

### 4. Page-Count Reconciliation

- [x] Add a static PDF fixture specifically chosen to be awkward for the existing regex-based page-count detection.
- [x] Prefer a fixture containing compressed object streams or an incremental update.
- [x] Record the true page count as a literal in the test.
- [x] Run the fixture through the full ingestion pipeline.
- [x] Assert that the final authoritative `totalPages` matches the known-correct page count.
- [x] Assert that the ingestion job reaches `done`.
- [x] Assert that `processedPages === totalPages`.
- [x] Ensure the test would fail if the pipeline incorrectly retained the unreliable regex count as the authoritative value.

### 5. Runtime Storage Cleanup

- [x] Verify test execution does not leave runtime artifacts under `data/` that become tracked.
- [x] Add a test/check ensuring no unexpected files are left under `data/` after the integration suite.
- [x] Ensure `.gitignore` continues to exclude `/data/`.

### 6. Verification

- [x] Run the AE-049 integration test suite.
- [x] Run the complete Vitest suite.
- [x] Run `npx tsc --noEmit`.
- [x] Run `npm run lint`.
- [x] Verify fixture files are present in Git.
- [x] Verify `git check-ignore` does not unexpectedly ignore the committed fixtures.
- [x] Verify CI runs successfully with the committed fixtures.

## Acceptance Criteria

- [x] Static PDF and image fixtures are committed under `src/__tests__/fixtures/`.
- [x] Fixtures are genuinely small and suitable for repeated CI execution.
- [x] PDF page counts in tests are known-correct literals rather than values generated by the production parser.
- [x] A real single-page PDF passes through the full pipeline and produces the expected Page document and stored asset.
- [x] A real multi-page PDF passes through the full pipeline and produces the expected number of Page documents and stored assets.
- [x] Image fixtures pass through the full pipeline and produce the expected single-page representation.
- [x] Mixed PDF/image inputs produce the correct pages and associations.
- [x] `(fileIndex, pageNumber)` ordering is verified through the full pipeline.
- [x] Retries/idempotent processing do not create duplicate Page documents or derived assets.
- [x] Invalid multi-file input follows the AE-048 reject-whole-batch behaviour.
- [x] The page-count reconciliation fixture proves that the authoritative renderer count is used.
- [x] The reconciliation fixture reaches `done` with `processedPages === totalPages`.
- [x] Fixtures are tracked by Git and are not silently swallowed by ignore rules.
- [x] Test execution does not result in stray tracked files under `data/`.
- [x] ESLint, TypeScript, the complete test suite, and CI pass.

## Out of Scope

- Core PDF rendering implementation (AE-046).
- Thumbnail generation implementation (AE-047).
- Mixed-input implementation itself (AE-048).
- Focused unit tests owned by AE-046, AE-047, or AE-048.
- UI implementation.
- Changes to unrelated authentication, authorization, or security behaviour.

## Sequencing

The Week 3 Day 2 sequence is:

**AE-046 → AE-047 → AE-048 → AE-049**

AE-046 establishes PDF rendering and the canonical page-image representation.

AE-047 establishes thumbnail generation.

AE-048 establishes mixed-input handling and deterministic file/page ordering.

AE-049 validates the complete pipeline using committed real-file fixtures.

## Working Protocol

- Small, focused commits.
- Keep ESLint + tests green.
- Verify TypeScript compilation before opening the PR.
- Demo at the Week 3 checkpoint.
- If a bug is found, use the Bug format:

  **Problem → What's happening → Error in logs → Where to look**
