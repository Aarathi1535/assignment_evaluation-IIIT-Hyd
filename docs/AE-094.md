# AE-094 — TA Assigned Scripts Work-Queue API

## Objective
The TA Work-Queue API retrieves all grading allocations assigned to the currently authenticated Teaching Assistant. It integrates with the existing permission and anonymizer subsystems to ensure strict student privacy (blind grading) on a per-exam basis while serving as the backend source for the grading work queue.

## Implementation
*   **API Route**: Exposes `GET /api/allocations`.
*   **TA Identification & Restriction**: Resolves the caller using `requirePermission(Permission.VIEW_ASSIGNED_SCRIPTS)`. The MongoDB query is strictly restricted to query allocations matching `ta: auth.user.id`, ensuring TAs can never retrieve allocations belonging to others.
*   **Optional `examId` Filter**: Parses the optional `examId` query string parameter. If provided, it validates the ObjectId format and filters allocations to return only those matching the specified exam. If omitted, it returns all allocations assigned to that TA across all courses/exams.
*   **Question-wise Context**: Incorporates question-wise metadata (`question` number) in the allocation return object, along with the current grading `status` (`PENDING`, `IN_PROGRESS`, `COMPLETED`).
*   **Permissions Enforced**: Enforces authentication and the existing `Permission.VIEW_ASSIGNED_SCRIPTS` permission at the route level.

## Anonymization and Privacy
*   **Anonymizer Integration**: Dynamically extracts nested `answerScript` objects from allocations and runs them through `Anonymizer.serializeAnswerScripts(scripts, viewer)`.
*   **Viewer Context**: Passes the authenticated caller's identity and role context (e.g. `{ id: auth.user.id, role: auth.user.role }`) to the anonymizer.
*   **Per-Exam Resolution**: Blind-grading behavior is resolved per script/exam automatically by the bulk serializer.
*   **Blind Mode Restrictions**: Under blind-grading active exams, student identification details (specifically `student`, `qrStudentId`, `omrStudentId`, and `candidateStudentId`) are entirely omitted from the response.

## API Behaviour

### Request
*   **Method**: `GET`
*   **Path**: `/api/allocations`
*   **Query Parameters**:
    *   `examId` (optional, string): Valid MongoDB ObjectId string representing the exam to filter allocations by.

### Response

#### Success (200 OK)
```json
{
  "success": true,
  "message": "Allocations retrieved successfully",
  "data": [
    {
      "_id": "60c72b2f9b1d8a001f8e1234",
      "exam": "60c72b2f9b1d8a001f8e1235",
      "status": "PENDING",
      "question": 2,
      "answerScript": {
        "_id": "60c72b2f9b1d8a001f8e1236",
        "exam": "60c72b2f9b1d8a001f8e1235",
        "anonymousId": "ANON-POTTER-777",
        "scriptReference": "Script #ANON-POTTER-777",
        "startPageNumber": 1,
        "endPageNumber": 4,
        "pageCount": 4,
        "isActive": true,
        "createdAt": "2026-08-27T08:00:00.000Z",
        "updatedAt": "2026-08-27T08:00:00.000Z"
      }
    }
  ]
}
```

#### Error Responses
*   **400 Bad Request**: Raised when `examId` is specified but has an invalid MongoDB ObjectId format.
    ```json
    {
      "success": false,
      "message": "Invalid Exam ID format",
      "data": null
    }
    ```
*   **401 Unauthorized**: Raised when no session cookie is supplied.
*   **403 Forbidden**: Raised when the authenticated user does not possess `Permission.VIEW_ASSIGNED_SCRIPTS` (e.g. students or professors).

## Validation

### Tests Added
Created [TaWorkQueueApi.test.ts](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/src/__tests__/TaWorkQueueApi.test.ts) covering:
1.  Access rejection for unauthenticated clients, students, and professors.
2.  Retrieval restriction matching only the authenticated TA.
3.  Optional `examId` filtering.
4.  Retrieval across multiple exams when no filter is provided.
5.  Question-wise allocation context matching.
6.  Integration with the `Anonymizer` system.
7.  Strict allowlist checks on blind-mode answer scripts.
8.  Exclusion of `student`, `qrStudentId`, `omrStudentId`, and `candidateStudentId` fields on blind-graded script payloads.
9.  Return of detailed un-anonymized payloads for non-blind exams.
10. Mixed-mode exam resolution in a single list response.

### Command Execution Results
*   **Focused Tests run (`npx vitest run src/__tests__/TaWorkQueueApi.test.ts`)**:
    ```powershell
    ✓ src/__tests__/TaWorkQueueApi.test.ts (10 tests) 3877ms
    Test Files  1 passed (1)
         Tests  10 passed (10)
    ```
*   **TypeScript Compilation (`npx tsc --noEmit`)**: Exited successfully with code `0`.
*   **ESLint Linter (`npm run lint`)**: Exited successfully with code `0` errors.
*   **Formatting Check (`git diff --check`)**: Exited successfully with code `0`.

## Out of Scope
*   Creating new permissions, database schemas, or database models.
*   Implementing frontend components, work queue UI dashboards, or grader screens.
*   Modifying or altering anonymization rules inside `src/lib/anonymizer.ts`.
