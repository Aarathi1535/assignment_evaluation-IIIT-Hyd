# AE-097 — TA Work-Queue Sorting

## Objective
Implement oldest-first queue sorting for the TA grading work queue integrated with pagination. The allocations returned to the authenticated TA must be sorted oldest first by default, sorted deterministically, and paginated. If a TA requests sorting explicitly (via query parameters), the backend must support oldest-first ordering, but reject any other sorting schemes (like difficulty/complexity sorting) with HTTP 400 Bad Request.

## Implementation Details

### API Endpoint: `GET /api/allocations`
- **TA Restriction**: Enforces permissions check at the route level via `requirePermission(Permission.VIEW_ASSIGNED_SCRIPTS)`. The query isolates allocations strictly matching `ta: auth.user.id`.
- **Pagination support**: Parse and validate `page` and `limit` query parameters. Clamp limit to a maximum of `100` and default to `page = 1`, `limit = 20`.
- **Default & Explicit Sorting**: Supports `sort` or `sortBy` query parameters.
  - Valid sort parameter values: `'oldest'`, `'oldest-first'`, or `'createdAt'`.
  - These values sort the allocations by `createdAt` (oldest first, ascending) and `_id` (ascending) to guarantee deterministic sorting across paginated page boundaries.
- **Invalid Sort Rejection**: If an invalid sort value (e.g. `'newest'`, or `'difficulty'`) is passed, the endpoint returns an HTTP 400 Bad Request response with a validation error message.
- **Filters Preservation**: Supports existing filtering parameters such as `examId` and `status` seamlessly, matching the query criteria prior to sorting.

## Deferral of Difficulty-Based Sorting (AE-097b)

> [!IMPORTANT]
> **Deferred Work (AE-097b)**:
> No difficulty or complexity metrics (such as page count, file size, question count, marks, etc.) currently exist in the database schemas or models.
> Therefore, difficulty-based sorting has been deferred to future ticket `AE-097b`.
> The backend explicitly rejects any sort parameter values that refer to difficulty, complexity, or other unsupported attributes with an HTTP 400 Bad Request error. We do not invent custom proxy metrics (like estimating difficulty from page count) to avoid introducing false assumptions.

## Response Shape Example

### Success (200 OK) with Default/Explicit Oldest-First Sort & Pagination
```json
{
  "success": true,
  "message": "Allocations retrieved successfully",
  "data": {
    "allocations": [
      {
        "_id": "60c72b2f9b1d8a001f8e1234",
        "exam": "60c72b2f9b1d8a001f8e1235",
        "status": "PENDING",
        "question": 2,
        "answerScript": {
          "_id": "60c72b2f9b1d8a001f8e1236",
          "exam": "60c72b2f9b1d8a001f8e1235",
          "anonymousId": "ANON-POTTER-777",
          "scriptReference": "Script #ANON-POTTER-777",
          "startPageNumber": 1,
          "endPageNumber": 4,
          "pageCount": 4,
          "isActive": true,
          "createdAt": "2026-08-27T08:00:00.000Z",
          "updatedAt": "2026-08-27T08:00:00.000Z"
        }
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 20,
      "total": 1,
      "totalPages": 1,
      "hasNextPage": false,
      "hasPreviousPage": false
    }
  }
}
```

### Invalid Sort Value (400 Bad Request)
```json
{
  "success": false,
  "message": "Invalid sort value: difficulty",
  "data": null
}
```

## Validation & Verification

### Focused Unit/API Tests
The test file [`TaQueueSorting.test.ts`](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/src/__tests__/TaQueueSorting.test.ts) verifies the sorting and pagination capabilities.
The tests cover:
1. Retrieval restricted to only the authenticated TA's allocations.
2. Default oldest-first ordering by `createdAt` ascending and `_id` ascending.
3. Explicit oldest-first sorting (`sort=oldest`).
4. Rejection of invalid sort parameters (e.g. `sort=difficulty`) with HTTP 400.
5. Verification that status and exam filters work correctly in tandem with sorting.
6. Anonymization behavior inside the queue output.
7. Omission of any mock or real difficulty metric.
8. Deterministic oldest-first sorting across multiple pages.
9. Pagination metadata correctness assertions.
10. Rejection of invalid pagination parameters with HTTP 400.
