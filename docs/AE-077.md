# AE-077: Ingestion Module Documentation + Runbook

Concise summary of documentation and handover deliverables for the **Ingestion Module (AE-077)**.

## 1. Goal
Provide a comprehensive, handover-ready runbook detailing the ingestion pipeline steps, data models, original vs derived assets, page classification/script assembly, student identification precedence, OMR templates coordinate structures, review dashboard counts aggregate logic, ingestion approval gates, cryptographic assembly sealing, correction freezes, and allocation gates.

## 2. Deliverables Implemented

### [INGESTION.md](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/docs/INGESTION.md)
A comprehensive engineering runbook covering:
1. **Purpose and Scope** of the ingestion boundaries.
2. **High-Level End-to-End Flow** (Mermaid architecture diagram).
3. **Core Data Models** (`Batch`, `IngestionJob`, `IngestionPage`, `AnswerScript`, `Exam`, `AuditLog`).
4. **Original vs Derived Assets** (distinguishing immutable raw uploads from mutable previews).
5. **Enhancement and Determinism** (assuring repeatable outcomes).
6. **Page Classification and Script Assembly** (blank detection, duplicate flagging, interactive corrections).
7. **Student Identification Precedence** (Operator Override > QR Code > OMR Bubble reader, conflict tracking).
8. **OMR Templates and Detection** (normalized bounds, schema coordinates, execution sequence).
9. **Review Dashboard** (faceted server aggregation counts, blank definition).
10. **Ingestion Approval Workflow** (approval gates, status transitions, batch resets).
11. **Assembly Seal and Verification** (cryptographic HMAC verification, rotation, audit tracking).
12. **Correction Freeze** (APPROVED status blocks composition adjustments).
13. **Allocation / Grading Gate** (blocks allocation on unapproved exams).
14. **Operational Runbook** (step-by-step procedures).
15. **Troubleshooting Guides** (stuck jobs, OMR misalignment, precedence overrides, mismatch checks).
16. **Design Decisions Matrix** (mentor-requested rationales).
17. **Testing and Verification** (commands and validation instructions).

### [AE-077.md](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/docs/AE-077.md)
This ticket summary document.

## 3. Key Design Decisions Documented
- **Raw content immutability** vs derived asset mutability.
- **Operator overrides** taking priority over automated checks.
- **OMR bubble matching** executed on all cover pages to record conflict diagnostics.
- **Faceted MongoDB aggregation** optimization for review metrics.
- **APPROVED composition freeze** to protect grading stability.

## 4. Verification Performed
- Verified all unit and integration test suites in the repository pass.
- Verified TypeScript compile (`npx tsc --noEmit`) passes with 0 errors.
- Verified ESLint linter (`npm run lint`) passes with 0 errors.
- Verified `git diff --check` passes with no whitespace issues.
