# AE-096 — Claim/Lock and Release Allocated Script

## Objective
Implement atomic, secure claim (lock) and release operations for allocated answer scripts. This allows teaching assistants (TAs) to safely claim pending scripts for grading (locking them against concurrent claims by other TAs) and release them back to pending if they cannot finish grading, allowing recovery without changing script ownership.

## Implementation

### 1. Database Operations
Modified [`src/services/AllocationService.ts`](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/src/services/AllocationService.ts):
*   **Atomic Claim (`claimAllocation`)**:
    Transitions an allocation's status from `PENDING` to `IN_PROGRESS` atomically matching the allocation ID, the TA ID, and status `PENDING` using Mongoose `findOneAndUpdate`. Runs inside a transaction.
*   **Atomic Release (`releaseAllocation`)**:
    Transitions an allocation's status from `IN_PROGRESS` back to `PENDING` using `findOneAndUpdate`. Supports a `isBackupOperator` flag allowing course Professors/Admins to release allocations belonging to other TAs. Runs inside a transaction.
*   **Conflict and Error Handling**:
    *   Returns `404 Not Found` if the allocation does not exist.
    *   Returns `403 Forbidden` if a TA attempts to claim or release an allocation belonging to another TA.
    *   Returns `409 Conflict` if the allocation has already been claimed (status is `IN_PROGRESS`).
    *   Returns `400 Bad Request` if attempting to claim or release `COMPLETED` allocations.

### 2. Audit Trail Logging
Both operations run inside database transactions to ensure that audit logging is transaction-safe and occurs if and only if the state transition succeeds:
*   **Claim Action (`ALLOCATION_CLAIM`)**:
    Logs successful transition from `PENDING` to `IN_PROGRESS`.
    - `user`: The acting TA ID.
    - `outcome`: `SUCCESS`
    - `details`: Includes `examId`, `answerScriptId`, `question`, and `taId`.
*   **Release Action (`ALLOCATION_RELEASE`)**:
    Logs successful transition from `IN_PROGRESS` to `PENDING`. Distinguishes self-release from Professor/Admin overrides.
    - `user`: The acting user's ID (the TA or Professor).
    - `outcome`: `SUCCESS`
    - `details`: Includes `examId`, `answerScriptId`, `question`, `owningTaId`, `actingUserId`, and `isOverride` (boolean).
    - **Professor Override**: When a Professor or Admin releases another TA's allocation, the `isOverride` metadata property is set to `true`, and `actingUserId` records the Professor's ID while `owningTaId` records the original TA's ID.

### 3. API Routes
*   **Claim Route (`POST /api/allocations/[id]/claim`)**:
    Created at [`src/app/api/allocations/[id]/claim/route.ts`](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/src/app/api/allocations/[id]/claim/route.ts). Enforces `Permission.GRADE_SCRIPT` permission.
*   **Release Route (`POST /api/allocations/[id]/release`)**:
    Created at [`src/app/api/allocations/[id]/release/route.ts`](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/src/app/api/allocations/[id]/release/route.ts). Authorizes users with either `Permission.GRADE_SCRIPT` (TAs, Admins) or `Permission.ALLOCATE_SCRIPTS` (Professors). Automatically identifies backup operators based on role (`PROFESSOR` or `ADMIN`).

## Concurrency
To guarantee concurrency safety, the database transitions rely exclusively on single-query atomic conditional updates (`findOneAndUpdate` matching the expected status). No vulnerable read-then-write logic is used. A dedicated concurrent-claims unit test verifies that when two identical claim requests arrive simultaneously, exactly one succeeds and the other receives a conflict response.

## Validation

### Focused Tests Added
Created [`src/__tests__/TaClaimReleaseApi.test.ts`](file:///c:/Users/AARATHISREE/Desktop/IIIT%20Hyd%20-%20Assignment%20Evaluation/Project%20Repo/assignment-evaluator/src/__tests__/TaClaimReleaseApi.test.ts) covering:
1.  Successful claim transition `PENDING -> IN_PROGRESS` (whole-script and question-wise).
2.  Duplicate claims rejection (409 Conflict).
3.  Claiming completed allocations rejection.
4.  Preventing TAs from claiming another TA's allocation.
5.  Two simultaneous claims result in exactly one success and one conflict.
6.  Successful release transition `IN_PROGRESS -> PENDING`.
7.  Preventing another TA from releasing the allocation.
8.  Preventing releasing completed allocations.
9.  Re-claiming a released allocation.
10. Allowing Professors/Admins to release other TAs' allocations.
11. Verification that claims/releases preserve ownership.
12. Audit logging for successful claims (`ALLOCATION_CLAIM`).
13. Audit logging for successful self-releases (`ALLOCATION_RELEASE` with `isOverride: false`).
14. Audit logging for successful Professor override releases (`ALLOCATION_RELEASE` with `isOverride: true`).
15. Verification that failed claims do not write audit logs.
16. Verification that failed releases do not write audit logs.
